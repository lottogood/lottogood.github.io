# LottoGood 향후 계획

## 운영자 메시지 관리

현재 첫 화면의 환영 레이어는 샘플 문구를 코드에 직접 넣어 모든 방문자에게 노출한다.

향후에는 운영자만 로그인할 수 있는 관리 화면을 추가해 다음을 관리한다.

- 노출 문구와 수신자 호칭
- 노출 시작·종료 시각, 페이지별 노출 여부
- 한 방문자당 1회 표시 등 노출 빈도
- 미리보기와 즉시 비활성화

정적 GitHub Pages만으로는 안전한 관리자 인증과 비공개 편집을 제공할 수 없으므로, 이 단계에서는 별도 인증·저장 기능을 갖춘 관리 백엔드(예: Cloudflare Workers 또는 Vercel + 데이터베이스)를 사용한다.

## 번호 생성 방식 확장

- 무작위 번호 생성: 1~45에서 중복 없이 6개 선택
- 오늘의 행운 조합: 생년월일·선택 시간·오늘 날짜를 브라우저 안에서 계산해 1~3개의 행운 숫자와 보완 숫자를 생성
- 모든 생성기는 오락·참고용으로 표시하고 당첨 예측 또는 확률 향상으로 표현하지 않음
- 생년 정보는 서버 전송·브라우저 저장 없이 계산 직후 폐기

## 홈 화면 설치(PWA)

- 모바일에서만 홈 화면 추가 안내를 표시하고, 설치된 독립 실행 모드에서는 숨김
- Android Chromium 계열에서는 브라우저 설치 확인창을 호출
- iPhone·iPad와 설치 확인창을 제공하지 않는 브라우저에서는 브라우저 메뉴의 수동 추가 방법을 안내
- 사용자 동의 없이 바로가기 아이콘을 자동 생성하지 않음
